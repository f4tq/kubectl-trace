FROM alpine:3.11
RUN apk add --update \
    bash \
    bc \
    build-base \
    curl \
    elfutils-dev elfutils-dev \
    linux-headers \
    bison \
    flex \
    make

WORKDIR /

COPY /init/fetch-linux-headers.sh /

ENTRYPOINT [ "/fetch-linux-headers.sh" ]
